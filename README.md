# AuthSphere 🔐

AuthSphere is a modern authentication and authorization server built with **Spring Boot**.  
It provides secure, scalable, and extensible identity management for microservices and enterprise applications.

---

## 🚀 Features
- **OAuth 2.1 & OpenID Connect** support
- **Multi-Factor Authentication (MFA)**
- **Passwordless login** (magic links, OTPs)
- **Role-Based Access Control (RBAC)** and **Attribute-Based Access Control (ABAC)**
- **Token management** with JWT & refresh tokens
- **Redis integration** for session and token caching
- **API Gateway compatibility** for microservice architectures
- **Kubernetes-ready** deployment with Helm charts

---

## 📂 Project Structure
